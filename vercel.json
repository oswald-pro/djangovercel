{
    "builds": [{
        "src": "mysystem/wsgi.py",
        "use": "@oswald-pro/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "mysystem/wsgi.py"
        }
    ]
}
