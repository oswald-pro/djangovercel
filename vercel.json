{
    "builds": [{
        "src": "mysystem/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "mysystem/wsgi.py"
        }
    ]
}
